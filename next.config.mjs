// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   // Enable static export for cPanel deployment
//   output: "export",
//   trailingSlash: true,
//   images: {
//     unoptimized: true, // Required for static export
//   },

//   // Enable experimental features for better performance
//   experimental: {
//     optimizePackageImports: ["lucide-react", "motion"],
//   },

//   // Image optimization (simplified for static export)
//   images: {
//     unoptimized: true, // Required for static export
//   },

//   // Note: Headers and redirects don't work with static export
//   // These would need to be configured at the server level (cPanel)

//   // Enable SWC minification
//   swcMinify: true,
// };

// export default nextConfig;


/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
